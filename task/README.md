# Code Fix Task

Fix the bug in `main.py`.

The `largest(arr)` function must:
- Return the largest value in the array.
- Work with positive and negative integers.
- Work with arrays containing a single element.

Run the tests after fixing the code.
All tests must pass.